# render.yaml
services:
  - type: web
    name: ai-nomadroof-chatbox     # 👈 Matches your Render static site name
    env: static
    buildCommand: npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
